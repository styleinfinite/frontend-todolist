[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/api/*"
  to = "https://tu-backend-url.netlify.app/:splat"
  status = 200

# Si usas rutas en React, asegúrate de tener este archivo en public/_redirects:
# /*    /index.html   200
